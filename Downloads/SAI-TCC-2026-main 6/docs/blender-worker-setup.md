# Blender Worker Setup

This setup has been superseded by the separated runtime guide:

- `docs/runpod-separated-deployment.md`

Use that document for:
- API/worker split
- immutable versioned tags
- RunPod env vars
- no-heavy-rebuild code sync workflow
